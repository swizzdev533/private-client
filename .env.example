# Private Client does not require application secrets for local builds.
# Signing and update publishing are intentionally configured outside the repository.

# Association API (apps/association-api) — Vercel Blob token (set via vercel env).
# BLOB_READ_WRITE_TOKEN=
# PRESENCE_TTL_SECONDS=45
# RATE_LIMIT_PER_MINUTE=30
